# Production profile overrides
# All secrets and origins should come from environment variables.

jwt:
  expiration-ms: ${JWT_EXP_MS:28800000}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:8080}
